/** */

let colores = ['grey', 'red', 'blue', 'green', 'yellow', 'black'];

let palabras = e15(['KK', '27', 'green', 'blue', '23']);

let nuevo = [];
function e15(palabras){

    for(let i=0; i < colores.length; i++){//recorro colores

        for(let j=0; j < palabras.length; j++){//recorro palabras
            
        }


        //en nuevo guardo los colores
    }
    

}
